// Tailwind CSS v4 - config is optional with @tailwindcss/postcss
// Content paths are auto-detected, but we can specify them explicitly
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
};
